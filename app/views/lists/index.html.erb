<%= render 'flash' %>



<table class="table table-bordered">
 

  <tbody>
    <% @lists.each do |list| %>
      <% if list.completed %>
      <tr class="table-secondary">
       <td><%= list.description %><td>
          <center><p>Done!!!!</p>
        <td>
       <center><%= link_to 'Edit', edit_list_path(list), class: "btn btn-outline-secondary btn-sm" %><td>
       <center><%= link_to 'Delete', list, method: :delete, data: { confirm: 'Are you sure?', },
        class: "btn btn-outline-danger btn-sm" %>
      </tr>
      <% else %>
      <tr>
      <td><%= list.description %><td>
        <td>
       <center><%= link_to 'Edit', edit_list_path(list), class: "btn btn-outline-secondary btn-sm" %><td>
        <center><%= link_to 'Delete', list, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-danger btn-sm" %>
        
      <% end %>
    <% end %>
  </tbody>
</table>
<br/>
<br/>
<%= link_to 'Delete Marked Items', home_trash_em_path, class: 'btn btn-outline-secondary', data: { confirm: 'Are you sure?', } %>  
<%= link_to 'Delete All Items', home_trash_em_all_path, class: 'btn btn-outline-secondary', data: { confirm: 'Are you sure?' } %>





